0x01-shell_permissions:
This directory contains my scripts for learning about Linux file permissions and user roles. 
In this project, I am learning how to:
* Switch users using `su`.
* View effective usernames and groups.
* Change file ownership using `chown` and `chgrp`.
* Modify file and directory permissions using `chmod` in both symbolic and numeric modes.
