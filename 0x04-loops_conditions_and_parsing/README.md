# 0x04. Loops, conditions and parsing

This directory contains my scripts for learning about control flow in Bash scripting. 

In this project, I am practicing:
* Creating `for`, `while`, and `until` loops.
* Using conditional statements like `if`, `elif`, `else`, and `case`.
* Using file test operators to check file states.
* Parsing text data using the `cut` command.
